# {{PROJECT_NAME}} — 开发规则

{{ONE_LINER}}

本 git 仓库根目录是 `{{PROJECT_NAME}}/`。

**目录约定**：
- 源码与配置、`docs/`、`AGENTS.md` — **入 git**
- `data/` — 运行时生成的数据（含隐私）、`.env` — **不入 git**

## 0. 核心不变量（绝不破坏）

> 本项目「绝不破坏」的架构约束。违反的改动直接拒。

{{INVARIANTS_BLOCK}}

## 1. 核心工作流

**每完成一个操作，按这个顺序做三件事**：

1. **补齐文档** — `docs/Progress.md` 「变更日志」追加记录（现象 / 根因 / 决策 / 验证结果）必写；按第 2 节「文档同步规则」表判断是否同时要动 `docs/PLAN.md`（推进了某个 Phase / 改变了方案）/ `docs/DECISIONS.md`（关键决策：选型、取舍、方案反转）/ `docs/ARCHITECTURE.md` / `docs/DEPLOYMENT.md`
2. **代码与文档一起做一个原子 commit** — `git add` 同时包含本次改动的源码与上一步改的文档；message 要能看懂改了什么、为什么改（commit 前必看 `git diff --staged`）
3. **立即 push 当前分支** — commit 后立即 `git push` 当前 feature/wip 分支（无需确认；无 upstream 时用 `-u` 建立；无远端时按 §5.1 第 3 条提醒用户建远端）

「一个操作」的粒度 ≈ 一个原子 commit 能覆盖的改动。不要把多个独立改动攒成一个大 commit。

> **为什么是「先补文档、后一次提交」而不是「先 commit 再补文档」**（历史上反转过一次，留此脚注防止再次反转）：
> 1. 「先 commit 再写文档」在语义上必然制造至少一个『代码与文档不一致』的历史提交，会话若在此中断，得到的正是第 5 节明令禁止的「写了代码不写文档」的结尾状态；
> 2. 文档更新本就是同一个『操作』的一部分，同 commit 才符合本节自己写的「一个操作 ≈ 一个原子 commit」，分两个 commit 等于把粒度定义推翻；
> 3. 实证：多个真实项目的冒烟验证里，文档与代码同 commit 全程执行，历史干净、无中间不一致态。

## 2. 文档同步规则

| 改动类型 | 必改文件 |
|---|---|
| 新增/修改源码 | `docs/Progress.md`（实现记录）+ 必要时 `docs/PLAN.md`（阶段进度）|
| 修 bug / 踩坑 | `docs/Progress.md`（现象、根因、修复）|
| Phase 推进 | `docs/PLAN.md` + `docs/Progress.md` |
| `.env.example` / 部署脚本变化 | `docs/Progress.md` + `docs/DEPLOYMENT.md` |
| 模块间接口变化（HTTP API / WS payload）| `docs/Progress.md` + `docs/ARCHITECTURE.md`（接口表）|
| 依赖变化（go.mod / package.json 等）| `docs/Progress.md`（加了什么、为什么）|
| 需求变化 / 需求反转 | `docs/REQUIREMENTS.md`（唯一真相源，先改这里；分工注记：业务事实变化改 BUSINESS，产品范围变化改 REQUIREMENTS）+ `docs/Progress.md` |
| 业务规则 / 业务流程变化 | `docs/BUSINESS.md`（业务事实源，先改这里）+ `docs/Progress.md` |
| 关键决策（选型 / 取舍 / 方案反转 / 绕行）| `docs/DECISIONS.md`（What/Why/Changes）+ `docs/Progress.md` |
| 会议纪要 | `docs/MEETINGS.md`（原始归档）+ `docs/BUSINESS.md`（业务事实提炼）+ `docs/REQUIREMENTS.md`（产品需求提炼）+ `docs/REQUIREMENTS.md` 目标台账（需求类条目）|
| 对话中出现的需求/期望/不满 | `docs/REQUIREMENTS.md` 目标台账（当轮，回一行确认）|
| 造出可复用组件（通用件，非业务专属） | 组织的组件索引登记条目（核实真实路径后写）+ `docs/Progress.md` |

**原则**：文档是单一事实源。代码和文档不一致时，先判断哪个对，再把另一个改齐。

## 3. 修改后质量检查

1. 读一遍被改的文件，确认逻辑正确
2. 用 grep 扫相关文档里的过时信息（旧函数名、字段名、路径），一并更新
3. 确保文档里的代码片段 / 配置示例与实际代码一致
4. UTF-8 乱码扫描：`LC_ALL=C grep -l $'\xef\xbf\xbd' docs/*.md AGENTS.md`
5. 改动影响到 `.env.example` 时，确认本地 `.env`（不入 git）也同步过
6. 前端/交互改动后，装了 ui-sweep 的话跑一次（交互冒烟走查；该引擎是普通 node 脚本（sweep.mjs），不依赖 Claude Code 专属能力，Codex 侧同样可用），跑完在 `docs/Progress.md` 记一条走查记录（写明 `ui-sweep` 字样 + 日期 + 扫了几屏几击 + 结论，0 缺陷也要记）；真缺陷另按变更日志逐条记（现象/根因/修复）；拦截名单外的破坏性按钮用项目 DENY_EXTRA 增补后再跑

## 4. 代码约定

{{CODE_CONVENTIONS_BLOCK}}

**通用**：
- UTF-8，中文注释允许、鼓励
- 多模块时：各模块**禁止互相 import**，只通过 HTTP / WS 通信（保模块边界）

## 5. Git 提交规范

- Commit message 首行 ≤ 72 字符，`feat:` / `fix:` / `docs:` / `refactor:` / `test:` / `chore:` 前缀
- 中英文均可，语言一致性优先
- 大改动拆原子 commit
- commit 前必看 `git diff --staged`
- **从不提交**：`.env`、真实凭据、`node_modules/`、`data/`（隐私）、`*.db`
- **同一对话会话里不允许「写了代码不写文档」的结尾状态**

### 5.1 分支与备份策略

1. **分支即推**：日常开发一律在 feature/wip 分支进行，开工先切/建分支；每次 commit 后立即 push 当前分支到远端，无需确认——这就是实时异地备份
2. **main 门禁**（只拦用户在前端看得出来的改动）：merge 进 main（或直接在 main 上 commit）前先判一次——这次改动用户在前端能不能看出/测出差别？**能**（前端代码改动，或后端逻辑变化会改变前端交互/展示行为）→ 必须先获用户确认，请求确认时把改动后的页面贴出来给用户检查——所有截图合并成一个自包含 HTML（图片内嵌、浏览器直接打开、每张图配一行标题），发到会话界面供查看，并在正文写出可复制进浏览器的本地路径；光文字描述或散发单张截图不算；交付给用户前先自检：用读图工具逐张检查截图，专抓一眼可见的问题——布局错位、元素重叠、文字溢出或截断、乱码或占位文本、空白或缺数据的区块、明显样式丢失、报错信息；查出问题先修复、重新截图、复查，通过才交付；拿不准是毛病还是有意设计的不硬修，交付时在正文点名让用户定，并附一句检查结论（通过，或修了什么）；起不了应用、截不了图时停止推送并如实报告门禁受阻，不许退化成纯文字确认；**不能**（用户没法在前端测试的：纯后端内部实现与重构、文档、后端测试、脚本、CI、依赖升级、接口行为不变的改动）→ 直接合并并 push main，无需确认，合并后报告一句。两类合并后都删除已合并的远端 feature 分支；拿不准就按「能」处理，先问；直接在 main 上 commit 仍先按规则 1 切分支，本条只管要不要确认。本条覆盖各流程里「合并前一律询问」的默认步骤
3. **无远端兜底**：仓库没有 remote（或分支没有 upstream）时，首次 commit 后提醒用户建远端，避免「自动 push」静默失效

## 6. 密钥处理

**绝不能写进 md 或代码的真实值**（API key、token、密码、加密 key 等）全部从 `.env` 读取（不入 git）。`.env.example` 用占位值。

## 7. 计划与文档约定

- 与用户把设计聊透后，design spec 落 `docs/specs/YYYY-MM-DD-<主题>-design.md`
- **spec 经用户批准后直接实现，不再写实现计划文档**：按 spec 拆独立单元，用 `parallel-do` 分波并行 spawn 子代理实现（遵守 TDD）——互相独立的单元并行，有依赖的排下一波，每波完成即验收，主对话汇总修复
- spec 获批后在 `docs/PLAN.md` 的「## Spec 索引」区追一行：`- [<主题>](specs/<日期>-<主题>-design.md) — <一句话>`，PLAN.md 保持路线图索引、不装正文
- `parallel-do` 也用于 spec 流程之外的临时并行任务（互不相关的多文件改动、多路并行调研）；用户明确要求先写实现计划或串行执行时，按用户指定执行
- **跨天或一个批次/spec 做完即收尾、换新会话**：会话跨天、或一个批次/spec 做完，就是收尾点——主动写一段交接 recap（项目内工作写进 `docs/Progress.md` 变更日志并附下一步；不属于本项目的散活写 `~/.codex/handoffs/<日期-主题>.md`），然后明确提醒用户开新会话接着干，不在旧会话上续跑下一批次；旧会话里也不再用「继续／好的」式短指令推进新工作。Codex 关不掉用户当前的终端窗口，但交接必须先备好，用户重开 `codex` 即可无缝接上。依据：2026-09-01 一次 Claude Code 用量审计（长会话反复重建缓存的机制对 Codex 同样成立），跨天会话只占 3%，却消耗了全部 token 的 58%；前 100 次大额缓存重建里 98 次发生在跨天会话；超长会话里一条「继续」实测最高 1.22 亿 token
- 安全/加固类的 spec、派工 prompt、代码注释、commit message 用中性工程语言写"系统做什么"（校验、限流、过期、权限收紧），不写"防什么攻击"；威胁场景记录进 `docs/DECISIONS.md`，不进派工 prompt 与代码注释——防内容安全分类器整条拦截导致回炉

### 7.1 分工与验收纪律（`parallel-do` 分波并行一律照此）

三层分工固定不变：**主对话**指挥——拆任务、定波次、最终汇总与冲突裁决；**子代理**执行——分波并行落地各独立单元；**主对话**验收——逐任务对照 diff 核实，不采信子代理自述。多代理编排下的子代理没有 per-stage 可调的模型/思考档位旋钮（默认走会话模型；轻量角色只能在全局配置里统一调），分工的着力点因此不在"选更贵的模型"，而在**验收的严格程度**：

- **失败才加验收轮次，不预付**：写入单元第一轮验收按常规流程走（读 diff、核对边界、跑测试）；被打回的单元重跑时，才追加一轮独立验收
- **多镜头替代单轮加长**：需要更高置信度时，加开一轮换角度的验收（正确性 / 安全 / 边界与异常数据），不是让同一个子代理反复自证或把上下文堆得更长——同质复核共享盲区
- **后果覆盖严格度**：触及数据迁移、删除/覆写数据、鉴权与权限、支付、对外发布的单元，无论实现看起来多简单，验收都必须亲自读全量 diff，且检查项含"回滚路径存在且可执行"
- **多轮验收报差异**：同一单元的第二轮及以后验收，只报与上一轮的差异（新发现/推翻项），不重复复述；无新发现要明说并列出复核点
- **诊断/根因/排障用链式接力**：下一轮验收/复查的 prompt 显式附上一轮结论与被否决的假设，连续两轮无新发现才收；验收裁决类照旧用多镜头（见上"多镜头替代单轮加长"）
- **派写入子任务的 prompt 边界里点名生产红线**：FORBIDDEN FILES 清单、绝不重启共享服务、绝不读写生产数据、禁止 force push 与丢改动的历史改写、只改分给本子任务的文件
- 需要架构判断、方案取舍、最终裁决的工作不派子代理，留主对话做

> 差异注记：`/goal` 是 Claude Code 专属的会话内命令，codex 面没有；跑偏防护只靠目标台账与对账点，不依赖 `/goal`。

## 禁止事项

- 改代码不更新文档 / commit
- 在 md 文件里写真实凭据
- 把 `data/` 进 git
- 破坏第 0 节核心不变量
- 在没确认的情况下把用户在前端看得出来的改动并入 main/默认分支（merge 或直接 commit；前端看不出差别的改动按 §5.1 直接合并不问）
- 听到需求不登记目标台账（不登记视同没听见；拿不准按 open 登记，宁滥勿漏）
- 跨天或批次/spec 做完后不写交接 recap 就在旧会话上续跑下一批次（收尾点见 §7）

---

> **注**：本文件是稳定规则。Phase 进度、依赖清单、当前状态等高频变动内容请放 `docs/PLAN.md` / `docs/Progress.md`。
