# Requirements — {{PROJECT_NAME}}

> The product requirements document, this project's **single source of truth**: the architecture (`docs/ARCHITECTURE.md`) and implementation plans all defer to this document.
> When requirements change, edit here first, then sync downstream docs. Inputs: meeting notes (`docs/MEETINGS.md`), client/user feedback, review conclusions.

## Product Positioning

{{ONE_LINER}}

<!-- TBD: one paragraph making clear "for whom, solving what, replacing what"; is this a recording tool or a management tool -->

## Target Users & Roles

<!-- TBD: one line per user type: role name + usage scenario + permissions/identity policy -->

## Phased Roadmap

> If the underlying model is unstable, the layers above will need rework. Nail down the core data model first, then build upper-layer features, and do aggregation/reporting capabilities last.

| Phase | Scope | Purpose |
|------|------|------|
| MVP 1 | <!-- TBD --> | |

## System-Level Design (Foundations)

<!-- TBD: one subsection per system-level constraint (e.g., A. Identity & Login, B. Core Data Model, C. Concurrency & Conflict Strategy, D. Runtime Environment Assumptions). Use an "Entity | Description" two-column table for the core data model -->

## State Machines (if any)

<!-- TBD: unify action naming (avoid mixing synonymous buttons); list states and actions for each state machine -->

## Feature List

<!-- TBD: one subsection per feature, heading tagged with a size marker (L/S), containing key points and edge cases -->

## UI/UX Design Principles

<!-- TBD -->

## Acceptance Test Plan

<!-- TBD: group by feature domain, list executable acceptance scenarios (including edge cases and data consistency scenarios) -->

## Confirmed Decisions

<!-- TBD: decisions confirmed with the client/user, as a numbered list (#1, #2…); DECISIONS.md entries reference these numbers -->

## Appendix: Core Entity Relations (ERD Sketch)

<!-- TBD: ASCII diagrams preferred -->

## Goal Ledger (Inbox)

> Every requirement, expectation, or complaint that surfaces in conversation or meetings lands here first; once it matures, fold it into the formal requirements above — but keep even the trivial ones on record. **Not in the ledger = never said.**

| Date | Verbatim (record as closely as possible) | Source | Status | Closeout evidence / disposition |
|---|---|---|---|---|
| YYYY-MM-DD | (placeholder) "…" | some conversation/meeting | open | — |

Status: open (unaddressed) / done (implemented, evidence = commit/screenshot/spec clause) / dropped (explicitly abandoned, one line explaining why) / promoted (distilled into the formal requirements above, note the section name).
